wifi_ssid: "YOUR_WIFI"
wifi_password: "YOUR_PASSWORD"
# Fallback AP when Wi-Fi fails (min 8 chars — change from the default!).
ap_password: "heatwhisper01"
# OTA + web_server (:80, user admin) + encrypted native API.
# api_key: 32 random bytes as base64 — generate with:
#   python3 -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
ota_password: "CHANGE_ME_OTA"
web_password: "CHANGE_ME_WEB"
api_key: "REPLACE_ME_GENERATE_NEW_KEY"
# mqtt_broker: "YOUR_MQTT_BROKER"
# mqtt_user: "YOUR_MQTT_USER"
# mqtt_password: "YOUR_MQTT_PASSWORD"
