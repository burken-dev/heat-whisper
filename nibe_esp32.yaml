esphome:
  name: nibe-bridge
  friendly_name: Nibe Bridge
packages:
  base: !include packages/base.yaml
  hw: !include packages/esp32_base.yaml
external_components:
  - source:
      type: local
      path: components
